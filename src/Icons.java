import javax.swing.*;

public interface Icons {
    ImageIcon MARIO = new ImageIcon(Icons.class.getResource("/finite_walk.gif"));
    ImageIcon SHELL = new ImageIcon(Icons.class.getResource("/infinito.gif"));
}
